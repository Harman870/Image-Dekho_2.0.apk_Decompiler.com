package i4;

import android.content.Context;
import android.util.Log;
import java.util.HashMap;
import java.util.concurrent.Executors;
import r3.h;

public final class jg {
    static {
        if (r1 != 0) {
            StringBuilder sb = new StringBuilder();
            sb.append('[');
            for (String str : new String[]{"SmsRetrieverHelper"}) {
                if (sb.length() > 1) {
                    sb.append(",");
                }
                sb.append(str);
            }
            sb.append("] ");
        }
        new h("FirebaseAuth", (String) null);
        int i10 = 2;
        while (i10 <= 7 && !Log.isLoggable("FirebaseAuth", i10)) {
            i10++;
        }
    }

    public jg(Context context) {
        new HashMap();
        Executors.unconfigurableScheduledExecutorService(Executors.newScheduledThreadPool(1));
    }
}
